
## Extracted Intent (from Intent Extractor — pre-analyzed user query)
- Subject: ${subject}
- Key points: ${key_points}
- Intent class: ${intent_class}
- Search keywords (use these as `query` to stock_news when relevant): ${search_keywords}
- Notes: ${notes}

Trust this extraction — it has already parsed the user's natural language. Build the plan around these structured signals **plus** the raw user query (do not ignore either).
